# Insurance ETL Pipeline Environment Configuration
# Copy this file to .env and update with your database settings

# Database Connection Settings
DB_SERVER=localhost
DB_NAME=insurance_dw
DB_AUTH_TYPE=sql
DB_USER=your_username
DB_PASSWORD=your_password
DB_DRIVER=ODBC Driver 17 for SQL Server

# Alternative: Windows Authentication (comment out DB_USER and DB_PASSWORD above)
# DB_AUTH_TYPE=integrated

# Logging Configuration
LOG_LEVEL=INFO
